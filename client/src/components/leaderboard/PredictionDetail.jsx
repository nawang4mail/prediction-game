export default function PredictionDetail({ userId }) {
  // TODO: fetch and display this user's predictions (US-03)
  return (
    <tr>
      <td colSpan={3} className="bg-gray-50 px-4 py-3 text-xs text-gray-500">
        Prediction detail for user {userId} — coming soon
      </td>
    </tr>
  );
}
